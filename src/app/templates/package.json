{
  "name": "@clowder-generator/<%= npm_name %>",
  "version": "<%= npm_version %>",
  "description": "<%= npm_description %>",
  "files": [
    "generators"
  ],
  "keywords": [
    "yeoman-generator"
  ],
  "author": "leddzip",
  "license": "ISC",
  "scripts": {
    "clean": "rm -rf generators",
    "test": "jest --config jestconfig.json --passWithNoTests",
    "preit": "npm run build",
    "it": "cucumber-js",
    "postit": "npm run clean",
    "prebuild": "npm run clean",
    "build": "tsc && cp -r src/app/templates generators/app/templates"
  },
  "dependencies": {
    "@clowder-generator/utils": "^0.5.1",
    "yeoman-generator": "^5.7.0"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^8.9.1",
    "@types/chai": "^4.3.4",
    "@types/jest": "^29.2.4",
    "@types/yeoman-generator": "^5.2.11",
    "@types/yeoman-test": "^4.0.3",
    "chai": "^4.3.7",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4",
    "yeoman-environment": "^3.13.0",
    "yeoman-test": "^6.3.0"
  }
}
